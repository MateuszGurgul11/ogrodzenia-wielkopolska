rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Dostęp wyłącznie przez FastAPI (Firebase Admin SDK / service account).
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
